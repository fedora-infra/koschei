# This is a config file for Koschei frontend that can override values in default
# configuration in /usr/share/koschei/config.cfg. It is a python file expecting
# assignment to config dictionary which will be recursively merged with the
# default one. See the default configuration file for the documentation of all
# available options
config = {
    # database connection properties
    "database_config": {
        "database": "koschei",
        "username": "koschei",
        # "password": "",
        # "port": 5432,
    },
    "flask": {
        # secret key for authentication, you want to override this
        "SECRET_KEY": "REPLACE_ME",
    },
}
# vim: ft=python
