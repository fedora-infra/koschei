#!/bin/sh
set -e

set "$@" .
x="ref: HEAD"
while true; do
    case "$x" in
        ref:*) x=$(cat "$1/.git/${x/#?????/}");;
        *) break;;
    esac
done
echo "${x/%?????????????????????????????????/}"
